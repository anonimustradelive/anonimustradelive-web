config.php
panel/config.php
crypto_donors_cache.json
crypto_seen_txs.json
auth-tokens.json
*.backup.html
concurso/submissions.json
concurso/uploads/*
!concurso/uploads/.htaccess

# Backup local de la memoria de Claude (info sensible, nunca subir)
backup/

# Credenciales y llaves — NUNCA subir
mykey
mykey.pub
*.pem
*.ppk
ssh-hosting.txt

# Copias de seguridad locales de la landing (no se despliegan ni hacen falta en el repo)
puntozero/RESTAURAR-*.html

# Correos de la lista de espera de Punto Zerø: datos de personas,
# generados en runtime. Nunca al repo.
puntozero/leads.json
